import createReducers from './reducers';

describe('createReducers', () => {
    // it('should not allow to update value in an immutable object', () => {
    //     const object = Immutable({
    //         age: 25
    //     });
    //     try { object.age = 200; } catch (e) {}
    //     try { object.name = 'Lampiao'; } catch (e) {}
    //     expect(object.age).to.equal(25);
    //     expect(object.name).to.not.be.defined;
    // });
});
